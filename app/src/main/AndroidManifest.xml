<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projetointegrador.solidarize">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VIEW.CadastroEvento"></activity>
        <activity android:name=".VIEW.CadastroPedidosDeDoacao"></activity>
        <activity android:name=".VIEW.CadastroInstituicao" />
        <activity android:name=".VIEW.EdicaoCadastroPessoa" />
        <activity android:name=".VIEW.EdicaoCadastroInstituicao" />
        <activity android:name=".VIEW.EdicaoCadastroEvento" />
        <activity
            android:name=".VIEW.NavDrawMenu"
            android:label="@string/title_activity_nav_draw_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".VIEW.CadastroPessoa" />
        <activity android:name=".VIEW.SelecionaTipoCadastro" />
        <activity android:name=".VIEW.EsqueciSenha">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".VIEW.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>